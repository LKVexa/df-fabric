# Third-Party Notices — VEC1 shell

The v0.2.0 overhaul pulled **no** third-party source from the GitHub Junkyard: every change is original work (build-new). The Python standard library is the only runtime dependency of the shell. Bundled DF_* trees carry their own `LICENSE` files and are unmodified.
